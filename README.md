IMPORTANTE!!!!!!!!!
el index.html y el styles.css no se usan. El styles.css lo hicimos por inercia (no tiene nada) y el index.html es lo que ahora se llama home.html (antes de terminarlo) y ninguno de los 2 se borro no se por que
